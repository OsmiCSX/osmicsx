import colors from "../../data/colors.json";
import backgroundColor from "../../src/background-color";

test('bg-transparent namespace', () => {
  expect(backgroundColor['bg-transparent']).toEqual({ backgroundColor: colors['transparent'] })
})

test('bg-black namespace', () => {
  expect(backgroundColor['bg-black']).toEqual({ backgroundColor: colors['black'] })
})

test('bg-white namespace', () => {
  expect(backgroundColor['bg-white']).toEqual({ backgroundColor: colors['white'] })
})

test('bg-gray-100 namespace', () => {
  expect(backgroundColor['bg-gray-100']).toEqual({ backgroundColor: colors['gray-100'] })
})

test('bg-gray-200 namespace', () => {
  expect(backgroundColor['bg-gray-200']).toEqual({ backgroundColor: colors['gray-200'] })
})

test('bg-gray-300 namespace', () => {
  expect(backgroundColor['bg-gray-300']).toEqual({ backgroundColor: colors['gray-300'] })
})

test('bg-gray-400 namespace', () => {
  expect(backgroundColor['bg-gray-400']).toEqual({ backgroundColor: colors['gray-400'] })
})

test('bg-gray-500 namespace', () => {
  expect(backgroundColor['bg-gray-500']).toEqual({ backgroundColor: colors['gray-500'] })
})

test('bg-gray-600 namespace', () => {
  expect(backgroundColor['bg-gray-600']).toEqual({ backgroundColor: colors['gray-600'] })
})

test('bg-gray-700 namespace', () => {
  expect(backgroundColor['bg-gray-700']).toEqual({ backgroundColor: colors['gray-700'] })
})

test('bg-gray-800 namespace', () => {
  expect(backgroundColor['bg-gray-800']).toEqual({ backgroundColor: colors['gray-800'] })
})

test('bg-gray-900 namespace', () => {
  expect(backgroundColor['bg-gray-900']).toEqual({ backgroundColor: colors['gray-900'] })
})

test('bg-red-100 namespace', () => {
  expect(backgroundColor['bg-red-100']).toEqual({ backgroundColor: colors['red-100'] })
})

test('bg-red-200 namespace', () => {
  expect(backgroundColor['bg-red-200']).toEqual({ backgroundColor: colors['red-200'] })
})

test('bg-red-300 namespace', () => {
  expect(backgroundColor['bg-red-300']).toEqual({ backgroundColor: colors['red-300'] })
})

test('bg-red-400 namespace', () => {
  expect(backgroundColor['bg-red-400']).toEqual({ backgroundColor: colors['red-400'] })
})

test('bg-red-500 namespace', () => {
  expect(backgroundColor['bg-red-500']).toEqual({ backgroundColor: colors['red-500'] })
})

test('bg-red-600 namespace', () => {
  expect(backgroundColor['bg-red-600']).toEqual({ backgroundColor: colors['red-600'] })
})

test('bg-red-700 namespace', () => {
  expect(backgroundColor['bg-red-700']).toEqual({ backgroundColor: colors['red-700'] })
})

test('bg-red-800 namespace', () => {
  expect(backgroundColor['bg-red-800']).toEqual({ backgroundColor: colors['red-800'] })
})

test('bg-red-900 namespace', () => {
  expect(backgroundColor['bg-red-900']).toEqual({ backgroundColor: colors['red-900'] })
})

test('bg-orange-100 namespace', () => {
  expect(backgroundColor['bg-orange-100']).toEqual({ backgroundColor: colors['orange-100'] })
})

test('bg-orange-200 namespace', () => {
  expect(backgroundColor['bg-orange-200']).toEqual({ backgroundColor: colors['orange-200'] })
})

test('bg-orange-300 namespace', () => {
  expect(backgroundColor['bg-orange-300']).toEqual({ backgroundColor: colors['orange-300'] })
})

test('bg-orange-400 namespace', () => {
  expect(backgroundColor['bg-orange-400']).toEqual({ backgroundColor: colors['orange-400'] })
})

test('bg-orange-500 namespace', () => {
  expect(backgroundColor['bg-orange-500']).toEqual({ backgroundColor: colors['orange-500'] })
})

test('bg-orange-600 namespace', () => {
  expect(backgroundColor['bg-orange-600']).toEqual({ backgroundColor: colors['orange-600'] })
})

test('bg-orange-700 namespace', () => {
  expect(backgroundColor['bg-orange-700']).toEqual({ backgroundColor: colors['orange-700'] })
})

test('bg-orange-800 namespace', () => {
  expect(backgroundColor['bg-orange-800']).toEqual({ backgroundColor: colors['orange-800'] })
})

test('bg-orange-900 namespace', () => {
  expect(backgroundColor['bg-orange-900']).toEqual({ backgroundColor: colors['orange-900'] })
})

test('bg-yellow-100 namespace', () => {
  expect(backgroundColor['bg-yellow-100']).toEqual({ backgroundColor: colors['yellow-100'] })
})

test('bg-yellow-200 namespace', () => {
  expect(backgroundColor['bg-yellow-200']).toEqual({ backgroundColor: colors['yellow-200'] })
})

test('bg-yellow-300 namespace', () => {
  expect(backgroundColor['bg-yellow-300']).toEqual({ backgroundColor: colors['yellow-300'] })
})

test('bg-yellow-400 namespace', () => {
  expect(backgroundColor['bg-yellow-400']).toEqual({ backgroundColor: colors['yellow-400'] })
})

test('bg-yellow-500 namespace', () => {
  expect(backgroundColor['bg-yellow-500']).toEqual({ backgroundColor: colors['yellow-500'] })
})

test('bg-yellow-600 namespace', () => {
  expect(backgroundColor['bg-yellow-600']).toEqual({ backgroundColor: colors['yellow-600'] })
})

test('bg-yellow-700 namespace', () => {
  expect(backgroundColor['bg-yellow-700']).toEqual({ backgroundColor: colors['yellow-700'] })
})

test('bg-yellow-800 namespace', () => {
  expect(backgroundColor['bg-yellow-800']).toEqual({ backgroundColor: colors['yellow-800'] })
})

test('bg-yellow-900 namespace', () => {
  expect(backgroundColor['bg-yellow-900']).toEqual({ backgroundColor: colors['yellow-900'] })
})

test('bg-green-100 namespace', () => {
  expect(backgroundColor['bg-green-100']).toEqual({ backgroundColor: colors['green-100'] })
})

test('bg-green-200 namespace', () => {
  expect(backgroundColor['bg-green-200']).toEqual({ backgroundColor: colors['green-200'] })
})

test('bg-green-300 namespace', () => {
  expect(backgroundColor['bg-green-300']).toEqual({ backgroundColor: colors['green-300'] })
})

test('bg-green-400 namespace', () => {
  expect(backgroundColor['bg-green-400']).toEqual({ backgroundColor: colors['green-400'] })
})

test('bg-green-500 namespace', () => {
  expect(backgroundColor['bg-green-500']).toEqual({ backgroundColor: colors['green-500'] })
})

test('bg-green-600 namespace', () => {
  expect(backgroundColor['bg-green-600']).toEqual({ backgroundColor: colors['green-600'] })
})

test('bg-green-700 namespace', () => {
  expect(backgroundColor['bg-green-700']).toEqual({ backgroundColor: colors['green-700'] })
})

test('bg-green-800 namespace', () => {
  expect(backgroundColor['bg-green-800']).toEqual({ backgroundColor: colors['green-800'] })
})

test('bg-green-900 namespace', () => {
  expect(backgroundColor['bg-green-900']).toEqual({ backgroundColor: colors['green-900'] })
})

test('bg-teal-100 namespace', () => {
  expect(backgroundColor['bg-teal-100']).toEqual({ backgroundColor: colors['teal-100'] })
})

test('bg-teal-200 namespace', () => {
  expect(backgroundColor['bg-teal-200']).toEqual({ backgroundColor: colors['teal-200'] })
})

test('bg-teal-300 namespace', () => {
  expect(backgroundColor['bg-teal-300']).toEqual({ backgroundColor: colors['teal-300'] })
})

test('bg-teal-400 namespace', () => {
  expect(backgroundColor['bg-teal-400']).toEqual({ backgroundColor: colors['teal-400'] })
})

test('bg-teal-500 namespace', () => {
  expect(backgroundColor['bg-teal-500']).toEqual({ backgroundColor: colors['teal-500'] })
})

test('bg-teal-600 namespace', () => {
  expect(backgroundColor['bg-teal-600']).toEqual({ backgroundColor: colors['teal-600'] })
})

test('bg-teal-700 namespace', () => {
  expect(backgroundColor['bg-teal-700']).toEqual({ backgroundColor: colors['teal-700'] })
})

test('bg-teal-800 namespace', () => {
  expect(backgroundColor['bg-teal-800']).toEqual({ backgroundColor: colors['teal-800'] })
})

test('bg-teal-900 namespace', () => {
  expect(backgroundColor['bg-teal-900']).toEqual({ backgroundColor: colors['teal-900'] })
})

test('bg-blue-100 namespace', () => {
  expect(backgroundColor['bg-blue-100']).toEqual({ backgroundColor: colors['blue-100'] })
})

test('bg-blue-200 namespace', () => {
  expect(backgroundColor['bg-blue-200']).toEqual({ backgroundColor: colors['blue-200'] })
})

test('bg-blue-300 namespace', () => {
  expect(backgroundColor['bg-blue-300']).toEqual({ backgroundColor: colors['blue-300'] })
})

test('bg-blue-400 namespace', () => {
  expect(backgroundColor['bg-blue-400']).toEqual({ backgroundColor: colors['blue-400'] })
})

test('bg-blue-500 namespace', () => {
  expect(backgroundColor['bg-blue-500']).toEqual({ backgroundColor: colors['blue-500'] })
})

test('bg-blue-600 namespace', () => {
  expect(backgroundColor['bg-blue-600']).toEqual({ backgroundColor: colors['blue-600'] })
})

test('bg-blue-700 namespace', () => {
  expect(backgroundColor['bg-blue-700']).toEqual({ backgroundColor: colors['blue-700'] })
})

test('bg-blue-800 namespace', () => {
  expect(backgroundColor['bg-blue-800']).toEqual({ backgroundColor: colors['blue-800'] })
})

test('bg-blue-900 namespace', () => {
  expect(backgroundColor['bg-blue-900']).toEqual({ backgroundColor: colors['blue-900'] })
})

test('bg-indigo-100 namespace', () => {
  expect(backgroundColor['bg-indigo-100']).toEqual({ backgroundColor: colors['indigo-100'] })
})

test('bg-indigo-200 namespace', () => {
  expect(backgroundColor['bg-indigo-200']).toEqual({ backgroundColor: colors['indigo-200'] })
})

test('bg-indigo-300 namespace', () => {
  expect(backgroundColor['bg-indigo-300']).toEqual({ backgroundColor: colors['indigo-300'] })
})

test('bg-indigo-400 namespace', () => {
  expect(backgroundColor['bg-indigo-400']).toEqual({ backgroundColor: colors['indigo-400'] })
})

test('bg-indigo-500 namespace', () => {
  expect(backgroundColor['bg-indigo-500']).toEqual({ backgroundColor: colors['indigo-500'] })
})

test('bg-indigo-600 namespace', () => {
  expect(backgroundColor['bg-indigo-600']).toEqual({ backgroundColor: colors['indigo-600'] })
})

test('bg-indigo-700 namespace', () => {
  expect(backgroundColor['bg-indigo-700']).toEqual({ backgroundColor: colors['indigo-700'] })
})

test('bg-indigo-800 namespace', () => {
  expect(backgroundColor['bg-indigo-800']).toEqual({ backgroundColor: colors['indigo-800'] })
})

test('bg-indigo-900 namespace', () => {
  expect(backgroundColor['bg-indigo-900']).toEqual({ backgroundColor: colors['indigo-900'] })
})

test('bg-purple-100 namespace', () => {
  expect(backgroundColor['bg-purple-100']).toEqual({ backgroundColor: colors['purple-100'] })
})

test('bg-purple-200 namespace', () => {
  expect(backgroundColor['bg-purple-200']).toEqual({ backgroundColor: colors['purple-200'] })
})

test('bg-purple-300 namespace', () => {
  expect(backgroundColor['bg-purple-300']).toEqual({ backgroundColor: colors['purple-300'] })
})

test('bg-purple-400 namespace', () => {
  expect(backgroundColor['bg-purple-400']).toEqual({ backgroundColor: colors['purple-400'] })
})

test('bg-purple-500 namespace', () => {
  expect(backgroundColor['bg-purple-500']).toEqual({ backgroundColor: colors['purple-500'] })
})

test('bg-purple-600 namespace', () => {
  expect(backgroundColor['bg-purple-600']).toEqual({ backgroundColor: colors['purple-600'] })
})

test('bg-purple-700 namespace', () => {
  expect(backgroundColor['bg-purple-700']).toEqual({ backgroundColor: colors['purple-700'] })
})

test('bg-purple-800 namespace', () => {
  expect(backgroundColor['bg-purple-800']).toEqual({ backgroundColor: colors['purple-800'] })
})

test('bg-purple-900 namespace', () => {
  expect(backgroundColor['bg-purple-900']).toEqual({ backgroundColor: colors['purple-900'] })
})

test('bg-pink-100 namespace', () => {
  expect(backgroundColor['bg-pink-100']).toEqual({ backgroundColor: colors['pink-100'] })
})

test('bg-pink-200 namespace', () => {
  expect(backgroundColor['bg-pink-200']).toEqual({ backgroundColor: colors['pink-200'] })
})

test('bg-pink-300 namespace', () => {
  expect(backgroundColor['bg-pink-300']).toEqual({ backgroundColor: colors['pink-300'] })
})

test('bg-pink-400 namespace', () => {
  expect(backgroundColor['bg-pink-400']).toEqual({ backgroundColor: colors['pink-400'] })
})

test('bg-pink-500 namespace', () => {
  expect(backgroundColor['bg-pink-500']).toEqual({ backgroundColor: colors['pink-500'] })
})

test('bg-pink-600 namespace', () => {
  expect(backgroundColor['bg-pink-600']).toEqual({ backgroundColor: colors['pink-600'] })
})

test('bg-pink-700 namespace', () => {
  expect(backgroundColor['bg-pink-700']).toEqual({ backgroundColor: colors['pink-700'] })
})

test('bg-pink-800 namespace', () => {
  expect(backgroundColor['bg-pink-800']).toEqual({ backgroundColor: colors['pink-800'] })
})

test('bg-pink-900 namespace', () => {
  expect(backgroundColor['bg-pink-900']).toEqual({ backgroundColor: colors['pink-900'] })
})
