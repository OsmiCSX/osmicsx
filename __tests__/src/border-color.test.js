import colors from "../../data/colors.json";
import borderColor from "../../src/border-color";

test('border-transparent namespace', () => {
  expect(borderColor['border-transparent']).toEqual({ borderColor: colors["transparent"] })
})

test('border-black namespace', () => {
  expect(borderColor['border-black']).toEqual({ borderColor: colors["black"] })
})

test('border-white namespace', () => {
  expect(borderColor['border-white']).toEqual({ borderColor: colors["white"] })
})

test('border-gray-100 namespace', () => {
  expect(borderColor['border-gray-100']).toEqual({ borderColor: colors["gray-100"] })
})

test('border-gray-200 namespace', () => {
  expect(borderColor['border-gray-200']).toEqual({ borderColor: colors["gray-200"] })
})

test('border-gray-300 namespace', () => {
  expect(borderColor['border-gray-300']).toEqual({ borderColor: colors["gray-300"] })
})

test('border-gray-400 namespace', () => {
  expect(borderColor['border-gray-400']).toEqual({ borderColor: colors["gray-400"] })
})

test('border-gray-500 namespace', () => {
  expect(borderColor['border-gray-500']).toEqual({ borderColor: colors["gray-500"] })
})

test('border-gray-600 namespace', () => {
  expect(borderColor['border-gray-600']).toEqual({ borderColor: colors["gray-600"] })
})

test('border-gray-700 namespace', () => {
  expect(borderColor['border-gray-700']).toEqual({ borderColor: colors["gray-700"] })
})

test('border-gray-800 namespace', () => {
  expect(borderColor['border-gray-800']).toEqual({ borderColor: colors["gray-800"] })
})

test('border-gray-900 namespace', () => {
  expect(borderColor['border-gray-900']).toEqual({ borderColor: colors["gray-900"] })
})

test('border-red-100 namespace', () => {
  expect(borderColor['border-red-100']).toEqual({ borderColor: colors["red-100"] })
})

test('border-red-200 namespace', () => {
  expect(borderColor['border-red-200']).toEqual({ borderColor: colors["red-200"] })
})

test('border-red-300 namespace', () => {
  expect(borderColor['border-red-300']).toEqual({ borderColor: colors["red-300"] })
})

test('border-red-400 namespace', () => {
  expect(borderColor['border-red-400']).toEqual({ borderColor: colors["red-400"] })
})

test('border-red-500 namespace', () => {
  expect(borderColor['border-red-500']).toEqual({ borderColor: colors["red-500"] })
})

test('border-red-600 namespace', () => {
  expect(borderColor['border-red-600']).toEqual({ borderColor: colors["red-600"] })
})

test('border-red-700 namespace', () => {
  expect(borderColor['border-red-700']).toEqual({ borderColor: colors["red-700"] })
})

test('border-red-800 namespace', () => {
  expect(borderColor['border-red-800']).toEqual({ borderColor: colors["red-800"] })
})

test('border-red-900 namespace', () => {
  expect(borderColor['border-red-900']).toEqual({ borderColor: colors["red-900"] })
})

test('border-orange-100 namespace', () => {
  expect(borderColor['border-orange-100']).toEqual({ borderColor: colors["orange-100"] })
})

test('border-orange-200 namespace', () => {
  expect(borderColor['border-orange-200']).toEqual({ borderColor: colors["orange-200"] })
})

test('border-orange-300 namespace', () => {
  expect(borderColor['border-orange-300']).toEqual({ borderColor: colors["orange-300"] })
})

test('border-orange-400 namespace', () => {
  expect(borderColor['border-orange-400']).toEqual({ borderColor: colors["orange-400"] })
})

test('border-orange-500 namespace', () => {
  expect(borderColor['border-orange-500']).toEqual({ borderColor: colors["orange-500"] })
})

test('border-orange-600 namespace', () => {
  expect(borderColor['border-orange-600']).toEqual({ borderColor: colors["orange-600"] })
})

test('border-orange-700 namespace', () => {
  expect(borderColor['border-orange-700']).toEqual({ borderColor: colors["orange-700"] })
})

test('border-orange-800 namespace', () => {
  expect(borderColor['border-orange-800']).toEqual({ borderColor: colors["orange-800"] })
})

test('border-orange-900 namespace', () => {
  expect(borderColor['border-orange-900']).toEqual({ borderColor: colors["orange-900"] })
})

test('border-yellow-100 namespace', () => {
  expect(borderColor['border-yellow-100']).toEqual({ borderColor: colors["yellow-100"] })
})

test('border-yellow-200 namespace', () => {
  expect(borderColor['border-yellow-200']).toEqual({ borderColor: colors["yellow-200"] })
})

test('border-yellow-300 namespace', () => {
  expect(borderColor['border-yellow-300']).toEqual({ borderColor: colors["yellow-300"] })
})

test('border-yellow-400 namespace', () => {
  expect(borderColor['border-yellow-400']).toEqual({ borderColor: colors["yellow-400"] })
})

test('border-yellow-500 namespace', () => {
  expect(borderColor['border-yellow-500']).toEqual({ borderColor: colors["yellow-500"] })
})

test('border-yellow-600 namespace', () => {
  expect(borderColor['border-yellow-600']).toEqual({ borderColor: colors["yellow-600"] })
})

test('border-yellow-700 namespace', () => {
  expect(borderColor['border-yellow-700']).toEqual({ borderColor: colors["yellow-700"] })
})

test('border-yellow-800 namespace', () => {
  expect(borderColor['border-yellow-800']).toEqual({ borderColor: colors["yellow-800"] })
})

test('border-yellow-900 namespace', () => {
  expect(borderColor['border-yellow-900']).toEqual({ borderColor: colors["yellow-900"] })
})

test('border-green-100 namespace', () => {
  expect(borderColor['border-green-100']).toEqual({ borderColor: colors["green-100"] })
})

test('border-green-200 namespace', () => {
  expect(borderColor['border-green-200']).toEqual({ borderColor: colors["green-200"] })
})

test('border-green-300 namespace', () => {
  expect(borderColor['border-green-300']).toEqual({ borderColor: colors["green-300"] })
})

test('border-green-400 namespace', () => {
  expect(borderColor['border-green-400']).toEqual({ borderColor: colors["green-400"] })
})

test('border-green-500 namespace', () => {
  expect(borderColor['border-green-500']).toEqual({ borderColor: colors["green-500"] })
})

test('border-green-600 namespace', () => {
  expect(borderColor['border-green-600']).toEqual({ borderColor: colors["green-600"] })
})

test('border-green-700 namespace', () => {
  expect(borderColor['border-green-700']).toEqual({ borderColor: colors["green-700"] })
})

test('border-green-800 namespace', () => {
  expect(borderColor['border-green-800']).toEqual({ borderColor: colors["green-800"] })
})

test('border-green-900 namespace', () => {
  expect(borderColor['border-green-900']).toEqual({ borderColor: colors["green-900"] })
})

test('border-teal-100 namespace', () => {
  expect(borderColor['border-teal-100']).toEqual({ borderColor: colors["teal-100"] })
})

test('border-teal-200 namespace', () => {
  expect(borderColor['border-teal-200']).toEqual({ borderColor: colors["teal-200"] })
})

test('border-teal-300 namespace', () => {
  expect(borderColor['border-teal-300']).toEqual({ borderColor: colors["teal-300"] })
})

test('border-teal-400 namespace', () => {
  expect(borderColor['border-teal-400']).toEqual({ borderColor: colors["teal-400"] })
})

test('border-teal-500 namespace', () => {
  expect(borderColor['border-teal-500']).toEqual({ borderColor: colors["teal-500"] })
})

test('border-teal-600 namespace', () => {
  expect(borderColor['border-teal-600']).toEqual({ borderColor: colors["teal-600"] })
})

test('border-teal-700 namespace', () => {
  expect(borderColor['border-teal-700']).toEqual({ borderColor: colors["teal-700"] })
})

test('border-teal-800 namespace', () => {
  expect(borderColor['border-teal-800']).toEqual({ borderColor: colors["teal-800"] })
})

test('border-teal-900 namespace', () => {
  expect(borderColor['border-teal-900']).toEqual({ borderColor: colors["teal-900"] })
})

test('border-blue-100 namespace', () => {
  expect(borderColor['border-blue-100']).toEqual({ borderColor: colors["blue-100"] })
})

test('border-blue-200 namespace', () => {
  expect(borderColor['border-blue-200']).toEqual({ borderColor: colors["blue-200"] })
})

test('border-blue-300 namespace', () => {
  expect(borderColor['border-blue-300']).toEqual({ borderColor: colors["blue-300"] })
})

test('border-blue-400 namespace', () => {
  expect(borderColor['border-blue-400']).toEqual({ borderColor: colors["blue-400"] })
})

test('border-blue-500 namespace', () => {
  expect(borderColor['border-blue-500']).toEqual({ borderColor: colors["blue-500"] })
})

test('border-blue-600 namespace', () => {
  expect(borderColor['border-blue-600']).toEqual({ borderColor: colors["blue-600"] })
})

test('border-blue-700 namespace', () => {
  expect(borderColor['border-blue-700']).toEqual({ borderColor: colors["blue-700"] })
})

test('border-blue-800 namespace', () => {
  expect(borderColor['border-blue-800']).toEqual({ borderColor: colors["blue-800"] })
})

test('border-blue-900 namespace', () => {
  expect(borderColor['border-blue-900']).toEqual({ borderColor: colors["blue-900"] })
})

test('border-indigo-100 namespace', () => {
  expect(borderColor['border-indigo-100']).toEqual({ borderColor: colors["indigo-100"] })
})

test('border-indigo-200 namespace', () => {
  expect(borderColor['border-indigo-200']).toEqual({ borderColor: colors["indigo-200"] })
})

test('border-indigo-300 namespace', () => {
  expect(borderColor['border-indigo-300']).toEqual({ borderColor: colors["indigo-300"] })
})

test('border-indigo-400 namespace', () => {
  expect(borderColor['border-indigo-400']).toEqual({ borderColor: colors["indigo-400"] })
})

test('border-indigo-500 namespace', () => {
  expect(borderColor['border-indigo-500']).toEqual({ borderColor: colors["indigo-500"] })
})

test('border-indigo-600 namespace', () => {
  expect(borderColor['border-indigo-600']).toEqual({ borderColor: colors["indigo-600"] })
})

test('border-indigo-700 namespace', () => {
  expect(borderColor['border-indigo-700']).toEqual({ borderColor: colors["indigo-700"] })
})

test('border-indigo-800 namespace', () => {
  expect(borderColor['border-indigo-800']).toEqual({ borderColor: colors["indigo-800"] })
})

test('border-indigo-900 namespace', () => {
  expect(borderColor['border-indigo-900']).toEqual({ borderColor: colors["indigo-900"] })
})

test('border-purple-100 namespace', () => {
  expect(borderColor['border-purple-100']).toEqual({ borderColor: colors["purple-100"] })
})

test('border-purple-200 namespace', () => {
  expect(borderColor['border-purple-200']).toEqual({ borderColor: colors["purple-200"] })
})

test('border-purple-300 namespace', () => {
  expect(borderColor['border-purple-300']).toEqual({ borderColor: colors["purple-300"] })
})

test('border-purple-400 namespace', () => {
  expect(borderColor['border-purple-400']).toEqual({ borderColor: colors["purple-400"] })
})

test('border-purple-500 namespace', () => {
  expect(borderColor['border-purple-500']).toEqual({ borderColor: colors["purple-500"] })
})

test('border-purple-600 namespace', () => {
  expect(borderColor['border-purple-600']).toEqual({ borderColor: colors["purple-600"] })
})

test('border-purple-700 namespace', () => {
  expect(borderColor['border-purple-700']).toEqual({ borderColor: colors["purple-700"] })
})

test('border-purple-800 namespace', () => {
  expect(borderColor['border-purple-800']).toEqual({ borderColor: colors["purple-800"] })
})

test('border-purple-900 namespace', () => {
  expect(borderColor['border-purple-900']).toEqual({ borderColor: colors["purple-900"] })
})

test('border-pink-100 namespace', () => {
  expect(borderColor['border-pink-100']).toEqual({ borderColor: colors["pink-100"] })
})

test('border-pink-200 namespace', () => {
  expect(borderColor['border-pink-200']).toEqual({ borderColor: colors["pink-200"] })
})

test('border-pink-300 namespace', () => {
  expect(borderColor['border-pink-300']).toEqual({ borderColor: colors["pink-300"] })
})

test('border-pink-400 namespace', () => {
  expect(borderColor['border-pink-400']).toEqual({ borderColor: colors["pink-400"] })
})

test('border-pink-500 namespace', () => {
  expect(borderColor['border-pink-500']).toEqual({ borderColor: colors["pink-500"] })
})

test('border-pink-600 namespace', () => {
  expect(borderColor['border-pink-600']).toEqual({ borderColor: colors["pink-600"] })
})

test('border-pink-700 namespace', () => {
  expect(borderColor['border-pink-700']).toEqual({ borderColor: colors["pink-700"] })
})

test('border-pink-800 namespace', () => {
  expect(borderColor['border-pink-800']).toEqual({ borderColor: colors["pink-800"] })
})

test('border-pink-900 namespace', () => {
  expect(borderColor['border-pink-900']).toEqual({ borderColor: colors["pink-900"] })
})
